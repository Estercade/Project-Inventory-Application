<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" type="text/css" href="/styles/styles.css">
    <link rel="stylesheet" type="text/css" href="/styles/view.css">
    <link rel="icon" href="/assets/favicon.svg">
    <script type="text/javascript" src="/utilities/setTheme.js" defer></script>
    <title><%- title %></title>
</head>
<body>
    <a name="top" class="topAnchor"></a>
    <header class="header">
        <%- include("partials/header") %>
    </header>
    <main>
        <div class="titleContainer">
            <h1 class="title"><%- title %></h1>
        </div>
        <% if (locals.pokemon) { %>
            <ul class="inventoryItem">
                <div class="itemDetailsContainer">
                    <div class="imageContainer"><img src="<%= pokemon.image %>" alt="<%= pokemon.name %> image"></div>
                    <div class="itemDetailsWrapper">
                        <li class="itemDetail">Name: <%= pokemon.name %></li>
                        <li class="itemDetail">Generation: <%= pokemon.generation %></li>
                        <li class="itemDetail">Pok&#233dex number: <%= pokemon.pokedex_number %></li>
                        <li class="itemDetail">Type: <span class="typeContainer <%= pokemon.type1 %>"><%= pokemon.type1 %></span> <% if (pokemon.type2) { %> <span class="typeContainer <%= pokemon.type2 %>"><%= pokemon.type2 %></span> <% } %></li>
                        <li class="itemDetail">Rarity: <%= pokemon.rarity %></li>
                        <% if (pokemon.shiny !== true) { %>
                            <li aria-label="false">Shiny: &#10007;</li>
                        <% } else { %>
                            <li aria-label="true">Shiny: &#10004;</li>
                        <% } %>
                        <li class="itemDetail">Price: <%= pokemon.price %></li>
                    </div>
                    <div class="itemControls">
                        <a href="/edit/<%- pokemon.id %>" class="editLink">Edit</a>
                        <form action="/delete/<%= pokemon.id %>" method="POST">
                            <button type="submit" onclick="return confirm('Are you sure you want to delete this Pok&#233mon?');" class="deleteItemButton"><u>Delete</u></button>
                        </form>
                    </div>
                </div>
            </ul>
        <% } else { %>
            <p>Item not found.</p>
        <% } %>
        <a href="/" class="backToHomeLink">Back to home</a>
    </main>
    <footer>
        <a href="#top" class="linkToTop">Back to top</a>
        <%- include("partials/footer") %>
    </footer>
</body>
</html>
<script></script>