<nav class="navbar">
    <ul class="linksContainer">
        <% links.forEach((link) => { %>
            <li>
                <a href="<%= link.href %>">
                    <span><%= link.text %></span>
                </a>
            </li>
        <% }) %>
    </ul>
</nav>